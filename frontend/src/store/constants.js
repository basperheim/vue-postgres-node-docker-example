const state = () => ({});

const getters = {
  // You can define getters here if needed
};

const mutations = {
  // You can define mutations here if needed
};

const actions = {
  // You can define actions here if needed
};

export default {
  namespaced: true, //! Important
  state,
  getters,
  mutations,
  actions,
};
